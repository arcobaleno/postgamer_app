<div>
<%= form_for(@micropost) do |f| %>
	<%= f.text_area :content, placeholder: "Talk some Game...", :cols => "10", :rows => "3" %>

	<%= f.hidden_field :user_id, :value => current_user.id %>

	<%= f.submit "Post", class: "btn btn-mini btn-inverse" %>
<% end %>
</div>